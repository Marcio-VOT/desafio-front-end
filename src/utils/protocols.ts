export type Employee = {
  id: string;
  name: string;
  job: string;
  admission_date: string;
  phone: string;
  image: string;
};

export type EmployeeInfoProps = {
  open?: boolean | undefined;
};